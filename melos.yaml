name: audiflow

packages:
  - packages/**

command:
  bootstrap:
    hooks:
      # post: melos run codegen --no-select  # Temporarily disabled until Phase 2

# Scripts are defined in pubspec.yaml under the 'melos:' key
# This is required for Melos 7.x with Flutter workspaces
