name: audiflow_search
description: "Podcast search and discovery API client for Audiflow"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  audiflow_core:
    path: ../audiflow_core
  dio: ^5.9.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.15
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.6
  http_mock_adapter: ^0.6.1
  json_serializable: ^6.9.5
